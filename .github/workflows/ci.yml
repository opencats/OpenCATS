name: CI Build

on:
  push:
    branches:
      - master
      - develop
      - Feature-php8.2_compat
      - feature/migrate-ci-to-github-actions
  pull_request:
    branches:
      - master
      - develop
      - feature/migrate-ci-to-github-actions
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
    strategy:
      fail-fast: false
      matrix:
        php-version: ['7.2', '8.2']

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          tools: composer:v2

      - name: Install Dependencies
        run: composer install --no-progress --prefer-dist

      - name: Create Reports Directory
        run: mkdir -p reports

      - name: Run PHPUnit (Unit Tests)
        run: ./vendor/bin/phpunit --log-junit reports/unit-report.xml src/OpenCATS/Tests/UnitTests

      - name: Run Integration Tests (Docker)
        run: |
          cd docker/
          # Start environment
          docker compose -f docker-compose-test.yml up -d

          # Wait for database (mirroring your script's logic)
          docker compose -f docker-compose-test.yml exec -T php php modules/tests/waitForDb.php

          # Run Integration tests with reporting
          docker compose -f docker-compose-test.yml exec -T php ./vendor/bin/phpunit --log-junit /var/www/public/reports/integration-report.xml src/OpenCATS/Tests/IntegrationTests

          # Run the rest of the original script (Behat, etc.)
          docker compose -f docker-compose-test.yml exec -T php /var/www/public/test/runAllTests.sh
        continue-on-error: true

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v4
        if: always()
        with:
          report_paths: 'reports/*.xml'
          detailed_summary: true
          include_passed: true
          check_name: "Test Results - PHP ${{ matrix.php-version }}"

      - name: Shutdown Docker
        if: always()
        run: |
          cd docker
          docker compose -f docker-compose-test.yml down
